import React from "react";
import ReactDOM from "react-dom/client";
import Remake from './remakes/may/re4/re2/Remake'

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
      <Remake/>
  </React.StrictMode>
);
